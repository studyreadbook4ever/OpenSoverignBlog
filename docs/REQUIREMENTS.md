# Requirements traceability matrix

Status date: 2026-07-19

This is a living map from product requirements to repository evidence and
release gates. It describes the code that exists in this repository; a design
document, schema, manifest, or domain crate is not counted as an operational
feature until the reference server and user interface enforce it end to end.

OpenSoverignBlog remains an **architecture preview**. Rows marked
`Implemented` identify working vertical slices, not a claim that the whole
product is complete or production-ready.

Status meanings:

- `Implemented` — the reference composition exercises the requirement end to
  end and has relevant automated coverage.
- `Partial` — a useful implementation exists, but a material part of the
  requirement is absent or disconnected.
- `Contract only` — types, schemas, manifests, tests, or normative guidance
  exist, but the feature is not mounted in the reference runtime.
- `Not implemented` — no usable implementation exists yet.

Release-gate meanings:

- `Preview satisfied` — sufficient for the current architecture preview;
  regressions still block a release.
- `Public beta blocker` — must be resolved before advertising the reference
  distribution as a generally usable self-hosted blog engine.
- `1.0 blocker` — must be resolved before promising a stable, extensible 1.0
  contract.
- `Continuous` — an ongoing verification or maintenance obligation.

| Requirement | Current status | Evidence | Remaining work | Release gate |
| --- | --- | --- | --- | --- |
| R-01. Detachable, capability-scoped modules with safe absent states | **Partial.** Ten bundled official DLCs now have an operational add/enable/disable/upgrade/reconcile/remove lifecycle. Human intent, exact manifest hashes, engine compatibility, contiguous history, and the runtime projection are updated together; disabled/removed modules retain state and capabilities report active/available/degraded/misconfigured states. The preview deliberately resolves only official manifests compiled into the matching CLI. | [modular-kernel ADR](architecture/0001-modular-kernel.md), [plugin API and installation lock](../crates/plugin-api/src/lib.rs), [DLC lifecycle](../apps/cli/src/bootstrap/dlc_lifecycle.rs), [installation schemas](../schemas/installation-lock.v1.schema.json), [official manifests](../plugins/official/) | Add a reviewed third-party artifact registry/runtime, migration executors, process isolation and crash recovery before claiming a general plugin host. Continue deriving runtime features from the verified lock rather than accepting arbitrary names as proof of operation. | **Public beta blocker; 1.0 blocker** |
| R-02. Default single-owner deployment with no signup or comments | **Implemented** for the preview's three administrator choices. Personal bootstrap defaults to a modular access-key exchange and closed member registration; generic owner-bound OIDC or fully disabled remote administration are alternatives. Disabled mode exposes no remote Studio and uses a network-isolated server-local setup/list/publish CLI. Delivery opens migrated storage read-only and rejects every mutation. | [administrator auth](../apps/server/src/admin_auth.rs), [bootstrap CLI](../apps/cli/src/bootstrap.rs), [server-local CLI](../apps/cli/src/local.rs), [community composition](../apps/server/src/community.rs), [example configuration](../config.example.toml) | Add browser/container end-to-end coverage, credential recovery and audit events, and separately compiled compositions if physical route absence beyond conditional mounting is required. | **Preview satisfied; public-beta regression gate** |
| R-03. External authentication adapters | **Partial.** Administrator external mode implements a generic OIDC authorization-code flow with discovery, signed ID-token verification, state, PKCE, nonce, exact issuer/`sub` owner binding, and opaque local sessions. Member OAuth remains unavailable, and Firebase/email are only future adapters at the verified-identity boundary. | [administrator auth](../apps/server/src/admin_auth.rs), [configuration](operations/CONFIGURATION.md), [external-auth domain](../features/external-auth/src/lib.rs), [OpenAPI](../openapi/openapi.yaml) | Add end-to-end tests against representative providers, rotation/account-recovery policy, multi-administrator linking, audit events, and separately reviewed Firebase, email, or member adapters. | **Public beta blocker** for broad external-login claims |
| R-04. Optional RBAC with deny-by-default fallback | **Partial.** Community sites persist owner/editor/writer memberships; Studio repository operations enforce user+site+document scope with cross-tenant tests; and owners can manage collaborators in Studio. Writers and editors may draft while publication, appearance, and membership changes remain owner-only. | [community storage](../crates/storage-sqlite/src/lib.rs), [community handlers](../apps/server/src/community.rs), [Studio settings](../apps/web/src/app.tsx), [RBAC domain](../features/rbac/src/lib.rs) | Route every future moderation/plugin operation through the same scoped policy, add audit events and invitation lifecycle, and make the RBAC module genuinely detachable. | **Public beta blocker** for mature delegated workflows; **1.0 blocker** for the authorization contract |
| R-05. Optional comments and moderation | **Partial.** Authenticated Markdown comments are persisted against published community documents, rendered through the comment domain, listed separately with their own ETag, and displayed by the article UI. The initial policy immediately approves valid comments. | [comments handlers](../apps/server/src/community.rs), [comment storage](../crates/storage-sqlite/src/lib.rs), [article UI](../apps/web/src/public-pages.tsx) | Add rate/abuse controls, moderation queues and UI, edit/delete enforcement, export, pagination, and a composition where comments are fully absent. | **Public beta blocker** for open registration; **1.0 blocker** for installable moderation claims |
| R-06. Markdown-first, immutable revision history, and portable export | **Implemented** as a core vertical slice. Markdown is required, revisions are immutable snapshots, stale writes conflict, publishing is separate, and CLI export retains Markdown plus structured history. | [content contracts](../crates/kernel/src/content.rs), [repository port](../crates/kernel/src/ports.rs), [SQLite repository](../crates/storage-sqlite/src/lib.rs), [administration CLI](../apps/cli/src/main.rs), [content schema](../schemas/content-envelope.v1.schema.json) | Add contract fixtures that prove Rust serialization, JSON Schema, TypeScript types, and exports stay identical. Extend export when first-party assets are implemented. | **Preview satisfied; regression gate** |
| R-07. Optional author-intent HTML with a reader-selectable `.md` view | **Implemented** for stored articles. Intent HTML remains optional and untrusted; the Rust renderer sanitizes it, and both public surfaces offer an author-intent/Markdown choice. | [renderer](../crates/renderer/src/lib.rs), [public server view](../apps/server/src/main.rs), [TypeScript reader](../apps/web/src/public-pages.tsx), [TypeScript SDK](../packages/sdk/src/index.ts) | Add browser-level security and accessibility regression tests and make projection availability explicit in the content contract. Keep Markdown usable when intent rendering or a plugin fails. | **Preview satisfied; regression gate** |
| R-08. Optional ontology sidecar rather than mandatory ontology | **Partial.** Ontology statements can be omitted, revisioned, returned, and exported without changing Markdown semantics. | [ontology content types](../crates/kernel/src/content.rs), [content schema](../schemas/content-envelope.v1.schema.json), [Studio input](../apps/web/src/studio.tsx) | Add a separately selectable ontology feature with query/index APIs, provenance and approval workflow, schema migration rules, JSON Lines export, and tests proving a disabled ontology feature does not affect ordinary posts. | **1.0 blocker** for claiming a usable ontology module |
| R-09. Fine-grained local drafts, paste behavior, and AI memory/context control | **Partial.** Studio now presents a focused title/Markdown surface, direct toolbar, social-embed composer, local draft status, keyboard save/publish shortcuts, explicit publication review, immutable revision save, and home-pin settings. Intent/embed/ontology/history and memory controls remain available under an advanced section. | [Studio](../apps/web/src/studio.tsx), [AI2AI contracts](../crates/kernel/src/ai2ai.rs) | Add explicit conflict resolution for restored drafts, define redaction/exclusion reasons, and connect per-operation memory choices to future external model/provider calls. | **Public beta blocker** |
| R-10. Velog-informed clean-room HTML kernel, math, and typed embeds | **Partial.** Markdown and intent paths have a strict sanitizer, math uses self-hosted KaTeX, and typed embed references remain inert first-party facades by default. The optional social-embeds DLC validates YouTube/X identities; YouTube uses a privacy-enhanced click-to-load iframe and X remains a safe outbound card. | [clean-room reference](architecture/velog-clean-room.md), [renderer](../crates/renderer/src/lib.rs), [social embed policy](../apps/server/src/social_embeds.rs), [web reader](../apps/web/src/public-pages.tsx), [content styles](../apps/web/public/assets/osb-content.css) | Make math consistent on the standalone route; version every render asset; add explicit persisted consent receipts, richer typed components, compatibility fixtures, and graceful artifacts for additional removed providers. | **Public beta blocker** |
| R-11. Provider-neutral AI2AI discovery and safe revision proposals | **Partial.** Discovery, schema, provenance/context receipts, optimistic concurrency, idempotency, agent-attributed proposals, and separate publication exist. A thin read-default MCP stdio adapter exposes only the HTTP boundary, uses a separate static content-write token, and correctly makes publishing a separate tool. A2A conformance is not claimed. | [AI2AI entrypoint](../AI2AI.md), [MCP adapter](../apps/mcp/README.md), [AI2AI domain](../crates/kernel/src/ai2ai.rs), [server endpoints](../apps/server/src/main.rs), [A2A boundary](ai2ai/A2A-ADAPTER.md) | Add per-client issuance/revocation if multi-client isolation is required; then enforce data boundaries, provider/capability and cost budgets, audit artifacts, context receipts, and executable contract fixtures. | **Public beta blocker; 1.0 blocker** |
| R-12. AI-authored, task-specific automation | **Partial.** The core contains no embedded LLM, macro schema, prompt format, or script runtime. The thin MCP adapter offers read tools by default and optional content-only write tools through a separate scoped credential, so an external AI can build task-specific prompts/scripts and submit complete conflict-checked revisions without receiving administrator authority. | [MCP adapter](../apps/mcp/README.md), [AI2AI entrypoint](../AI2AI.md), [MCP implementation](../apps/mcp/src/lib.rs) | Add per-client credential issuance/revocation and externally hosted, reviewable proposal artifacts with budgets and last-good outputs; keep all prompt/script policy outside the publishing process. | **Public beta blocker** for the AI-native automation claim |
| R-13. Modular SEO and operator-controlled URLs | **Partial.** The reference server preserves reverse-proxy subpaths across the SPA, assets, API-generated media URLs, canonical URLs, discovery files, historical slug redirects, `robots.txt`, sitemap, Open Graph, Twitter metadata, and no-index mode. | [SEO policy](../features/seo/src/lib.rs), [server SEO routes](../apps/server/src/main.rs), [web base-path support](../apps/web/src/lib.tsx), [SEO manifest](../plugins/official/seo/plugin.toml) | Make SEO genuinely detachable instead of a mandatory server dependency; expose validated per-site metadata; add JSON-LD, generated social cards, and broader URL-collision tests. | **Public beta blocker** |
| R-14. Optional isolated code execution for Rust, Kotlin, Flutter, and other profiles | **Contract only.** A bounded request type, disabled client, security model, manifest, and conditional UI button exist. The button only emits a browser event. | [runner client contract](../features/code-runner-client/src/lib.rs), [runner security model](security/CODE-RUNNER.md), [runner manifest](../plugins/official/code-runner/plugin.toml), [web button](../apps/web/src/main.tsx) | Implement an authenticated remote-runner adapter, server job API, health-based capability state, output/artifact UI, cancellation and audit. Supply digest-pinned Rust/Kotlin/Flutter profiles and the security tests required by the normative model. Never execute code in the publishing process. | **Public beta blocker** for enabling the feature |
| R-15. Sovereign monetization slots and developer-friendly provider catalog | **Partial.** A provider schema, named-slot policy manifest, agent installation protocol, and one explicitly fictional direct-sponsor template exist. | [provider catalog guide](../providers/README.md), [provider schema](../providers/provider.schema.json), [fictional example](../providers/example-direct-sponsor.yaml), [AI2AI ad guide](ai2ai/AD-INTEGRATION.md) | Add independently verified provider entries with dated primary sources, eligibility, regions, APIs, domains, secrets, storage, payout and policy data. Implement first-party sponsorship/house-ad slots before adding third-party adapters. The repository does not currently contain the requested broad provider catalog. | **Public beta blocker** for monetization claims; **Continuous** verification |
| R-16. EU-oriented consent, disclosure, refusal, and withdrawal controls | **Contract only** at the runtime boundary. The legal profile, schemas, default-deny resource gate, tests, and a preview banner exist, but they are not one enforced system. | [EU consent profile](legal/EU-CONSENT.md), [consent policy schema](../schemas/consent-policy.v1.schema.json), [consent receipt schema](../schemas/consent-receipt.v1.schema.json), [ad disclosure schema](../schemas/ad-disclosure.v1.schema.json), [resource gate](../features/monetization-policy/src/lib.rs) | Load a validated active policy; present granular actor/purpose choices; store data-minimised receipts; make refusal and withdrawal durable; gate every network/storage operation; add clean-browser network tests. External HTTPS images currently remain loadable through the renderer/CSP without this gate and must be proxied, declared, or blocked. Do not claim legal certification. | **Public beta blocker** and **Continuous** legal/technical review |
| R-17. Rust core and detachable TypeScript UI/SDK | **Implemented** as the baseline technology split. | [Rust workspace](../Cargo.toml), [server](../apps/server/src/main.rs), [web application](../apps/web/src/app.tsx), [TypeScript SDK](../packages/sdk/src/index.ts), [CI](../.github/workflows/ci.yml) | Generate or contract-test API types across Rust, JSON Schema, and TypeScript; publish stable SDK artifacts; document compatibility and deprecation policy; keep composition logic out of the kernel. | **Preview satisfied; 1.0 compatibility gate** |
| R-18. SQLite-first, single-node sovereign storage | **Partial.** WAL, durability profiles, foreign keys, busy timeout, immutable revisions, route history, content-addressed assets, verified SQLite/blob generations, restore verification, portable export, and optional Redis-free origin reads are implemented. Redis is a signed expiring derivative; managed backup generations are the canonical recovery path. | [SQLite adapter](../crates/storage-sqlite/src/lib.rs), [asset store](../crates/assets-fs/src/lib.rs), [managed backup](../apps/server/src/backup.rs), [CLI backup/export/restore](../apps/cli/src/main.rs), [deployment guide](../deploy/README.md) | Add the documented job/outbox tables, measured concurrency tests, automated off-host transport, and recurring restore drills across a genuinely separate failure domain. | **Public beta blocker** |
| R-19. On-premise deployment and attachment to an existing site | **Partial.** Interactive/flag-complete bootstrap records auth, CSS, cache and exact DLC selection; doctor and runtime enforce the lock. The non-root, read-only container supports no Redis, standalone Redis, or primary/replica/Sentinel profiles, durable data/backups, delivery-only restores, local maintenance, health endpoints, and subpath-aware UI. A Linux updater verifies a signed semantic release, quiesced backup, fresh candidate volume, exact-version health, atomic control promotion, and last-known-good rollback. | [bootstrap and lifecycle CLI](../apps/cli/src/bootstrap.rs), [updater runbook](operations/UPDATES.md), [Dockerfile](../Dockerfile), [Compose](../compose.yaml), [deployment guide](../deploy/README.md) | Add tested TLS reverse-proxy recipes, metrics/alerts, real signed-release drills, disaster recovery across two physical hosts, and detachable renderer examples. | **Public beta blocker** |
| R-20. Secure treatment of JavaScript, TypeScript, HTML, model output, and embeds | **Partial.** Raw Markdown HTML is escaped, intent HTML is allowlisted in Rust and defensively sanitized in the browser, KaTeX trust is disabled, CSP is restrictive, and in-process code execution is prohibited. | [renderer](../crates/renderer/src/lib.rs), [web reader](../apps/web/src/main.tsx), [security policy](../SECURITY.md), [runner security model](security/CODE-RUNNER.md) | Close the external-image consent/network escape; add request and abuse controls appropriate to public deployment, browser XSS/Trusted Types tests, dependency and container scanning, threat-model regression fixtures, and security review for every active adapter. | **Public beta blocker; Continuous** |
| R-21. No mandatory embedded editor LLM | **Implemented by design.** Human editing and reading require no model, while external agents use the AI2AI API. | [README](../README.md), [Studio](../apps/web/src/studio.tsx), [AI2AI entrypoint](../AI2AI.md) | Keep the model-free path fully functional as optional external AI adapters are introduced. | **Preview satisfied; regression gate** |
| R-22. AI-readable adoption and operator documentation | **Partial.** Versioned discovery, `agent.txt`/`agents.txt`/`llms.txt`, OpenAPI, public and installation JSON Schemas, a TypeScript SDK, exact install/lock files, semantic configuration, AI instructions, a minimal MCP adapter, flag reference, and verified update runbook form a strong base. | [README](../README.md), [AI2AI entrypoint](../AI2AI.md), [MCP adapter](../apps/mcp/README.md), [OpenAPI](../openapi/openapi.yaml), [schema directory](../schemas/), [update runbook](operations/UPDATES.md) | Add executable request/response fixtures, per-client scoped machine-token issuance, plugin-author tutorials, stable release compatibility policy, and automated documentation link tests. | **Public beta blocker** |
| R-23. Unlicense project with license-safe clean-room provenance | **Partial.** Original project packages declare Unlicense, Rust and JavaScript dependencies have generated inventories and notices, and Velog is treated only as a pinned clean-room research source. | [Unlicense](../UNLICENSE), [dependency policy](../deny.toml), [application SBOM](legal/application-sbom.cdx.json), [third-party notices](../THIRD_PARTY_NOTICES.md), [Velog clean-room policy](architecture/velog-clean-room.md), [CI dependency check](../.github/workflows/ci.yml) | Generate and attest a final-image container SBOM for every immutable release digest; keep source/assets, generated output provenance, and notice obligations under continuous review. | **1.0 blocker; Continuous** |

## Release interpretation

The current preview demonstrates portable Markdown, immutable SQLite revisions,
optional intent and ontology data, safe rendering, three administrator modes,
optional Redis, exact installation locks, a bundled-official DLC lifecycle,
verified backup/restore/update boundaries, and a deliberately thin AI2AI/MCP
surface. It must not yet be described as a general third-party plugin host,
broad provider/member authentication platform, mature moderation system,
in-process code runner, advertising network, legal-certification system,
ontology query engine, or embedded AI macro runtime.

Before a public beta, the composition root must report only capabilities that
are actually configured and healthy, and the P0 runtime gaps above must be
closed with end-to-end tests. This matrix should be updated in the same change
that alters a requirement, contract, release claim, or implementation status.
