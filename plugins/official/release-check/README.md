# Release status check DLC

This bundled module performs a bounded, non-executing release metadata check
for `/api/v1/version`. Disable it with `OSB_UPDATE_CHECKS=false` for a fully
offline installation. Installing software remains the host-side updater's job.
