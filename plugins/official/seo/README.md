# SEO and URL policy

Optional pure projection module. It owns canonical/base-path policy, robots,
sitemap output, structured metadata, and explicit redirect status. Route
history remains portable data even when the feature is disabled. It never
changes Markdown and never lets a theme silently invent a different canonical
URL.
