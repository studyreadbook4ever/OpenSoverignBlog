# Comments

Optional UGC module. The default site has no comment routes, tables, scripts,
or identity requirement. When installed, comments enter a moderation state
machine and are sanitized independently from authored content. Disabling the
module hides routes and slots but preserves data; purge is a separate action.
